# Runtime configuration for scripts/manage-container.sh
# CONTAINER_NAME is the base name; the script appends "-dev" or "-test".
CONTAINER_NAME=fuseki
FUSEKI_PORT=3030
FUSEKI_CONFIG_VOLUME=/path/to/folder/with/configuration/files
REQUIRE_LUCENE=true
FUSEKI_ENDPOINT_HEALTH=/$/ping
